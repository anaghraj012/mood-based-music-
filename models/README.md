# Models

Emotion analysis uses DeepFace and its configured pre-trained backend. DeepFace may download and cache model resources on first use. Large model weights and caches should remain outside this repository; this project does not claim ownership of third-party models or training data.
