# Data

`emotions.json` contains the configurable emotion-to-mood mapping. Analysis history is written to `history/analysis_history.json`, and webcam captures are written to `captures/`. These generated files are ignored by Git and should not contain private photographs in a submission.
